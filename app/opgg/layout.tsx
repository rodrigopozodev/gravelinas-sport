/**
 * Sin nav aquí: `SiteSidebarNav` va dentro de `OpggDuoView` (flujo DOM encima del scroll).
 */
export default function OpggLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
